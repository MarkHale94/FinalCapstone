@model PharmaQueue.Models.CustomerViewModels.CustomerSearchViewModel

@{
    ViewData["Title"] = "Search";
}

<h2>Search Results for "@Model.Search"</h2>
<hr />
<p>Showing all customers with last names related to "@Model.Search"</p>
<hr />
@if (Model.Customers.Count() == 0)
{
    <p>No Customers match "@Model.Search"</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th class="col-md-2">
                    Customer Name
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Customers)
            {
                <tr>
                    <td class="col-md-2">
                        <a asp-controller="Customers" asp-action="Details" asp-route-id="@item.UserId" class="category-title">
                            @Html.DisplayFor(modelItem => item.FirstName)
                            @item.LastName
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}